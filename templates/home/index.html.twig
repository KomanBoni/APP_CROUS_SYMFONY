{% extends 'base.html.twig' %}

{% block title %}Delivcrous - Livraison de plats pour étudiants{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="/css/delivcrous.css">
{% endblock %}

{% block body %}
<header>
    <nav>
        <div class="logo"><a href="{{ path('app_home') }}" style="text-decoration: none; color: inherit;">Delivcrous</a></div>
        <ul class="nav-links">
            <li><a href="#accueil">Accueil</a></li>
            <li><a href="{{ path('app_etudiant') }}">Etudiant</a></li>
            <li><a href="{{ path('app_panier') }}">Panier</a></li>
            {% if app.user %}
                {% if 'ROLE_ADMIN' in app.user.roles %}
                    <li><a href="{{ path('app_plat_index') }}">Admin</a></li>
                {% endif %}
                <li>
                    <form method="post" action="{{ path('app_logout') }}" style="display: inline; margin: 0;">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                        <button type="submit" style="background: rgba(255,255,255,0.2); border: 2px solid white; color: white; font-weight: 500; cursor: pointer; padding: 0.4rem 1rem; border-radius: 20px; transition: all 0.3s; font-size: 0.9rem;" onmouseover="this.style.background='white'; this.style.color='#667eea';" onmouseout="this.style.background='rgba(255,255,255,0.2)'; this.style.color='white';">
                            🚪 Déconnexion
                        </button>
                    </form>
                </li>
            {% else %}
                <li><a href="{{ path('app_login') }}">Connexion</a></li>
                <li><a href="{{ path('app_register') }}">Inscription</a></li>
            {% endif %}
        </ul>
        <a href="#commander" class="btn-primary">Commander</a>
    </nav>
</header>

<section class="hero" id="accueil">
    <div class="hero-content">
        <h1>🍽️ Delivcrous</h1>
        <p>Vos plats préférés livrés directement sur votre campus !</p>
        <p style="font-size: 1.1rem; margin-top: -1rem;">Parfait pour les étudiants qui veulent manger bien sans se ruiner</p>
        <div class="hero-buttons">
            <a href="#commander" class="btn-secondary">Voir le menu</a>
            <a href="#avantages" class="btn-secondary">Découvrir</a>
        </div>
    </div>
</section>

<section class="features" id="avantages">
    <div class="container">
        <h2 class="section-title">Pourquoi choisir Delivcrous ?</h2>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">⚡</div>
                <h3>Livraison rapide</h3>
                <p>Recevez vos plats en moins de 30 minutes directement sur votre campus</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">💰</div>
                <h3>Prix étudiants</h3>
                <p>Des tarifs adaptés à votre budget avec des promotions régulières</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🍕</div>
                <h3>Menu varié</h3>
                <p>Pizzas, burgers, sushis, salades... Un large choix pour tous les goûts</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📱</div>
                <h3>Commande facile</h3>
                <p>Commandez en quelques clics depuis votre smartphone ou ordinateur</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🌱</div>
                <h3>Options végétariennes</h3>
                <p>Des plats délicieux pour tous les régimes alimentaires</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">⭐</div>
                <h3>Qualité garantie</h3>
                <p>Des plats préparés avec des ingrédients frais et de qualité</p>
            </div>
        </div>
    </div>
</section>

<section class="menu-preview" id="menu">
    <div class="container">
        <h2 class="section-title">Nos plats populaires</h2>
        <div class="menu-grid">
            {% for plat in plats %}
            <div class="menu-item">
                <div class="menu-item-image">
                    {% if plat.image %}
                        <img src="{{ plat.image }}" alt="{{ plat.nom }}">
                    {% else %}
                        <div style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); height: 100%; display: flex; align-items: center; justify-content: center; font-size: 4rem;">🍽️</div>
                    {% endif %}
                </div>
                <div class="menu-item-content">
                    <h4>{{ plat.nom }}</h4>
                    <p>{{ plat.description ?? '' }}</p>
                    <div class="price">{{ plat.prix }}€</div>
                </div>
            </div>
            {% else %}
            <p style="text-align: center; grid-column: 1 / -1; color: #666;">Aucun plat disponible pour le moment.</p>
            {% endfor %}
        </div>
    </div>
</section>

<section class="cta" id="commander">
    <h2>Prêt à commander ?</h2>
    <p>Rejoignez des milliers d'étudiants qui nous font confiance</p>
    <a href="#menu" class="btn-primary" style="display: inline-block; margin-top: 1rem;">Commander maintenant</a>
</section>

<footer id="contact">
    <div class="footer-content">
        <div class="footer-links">
            <a href="#accueil">Accueil</a>
            <a href="#menu">Menu</a>
            <a href="#avantages">Avantages</a>
            <a href="#">CGV</a>
            <a href="#">Mentions légales</a>
            <a href="#">Contact</a>
        </div>
        <p>&copy; 2024 Delivcrous - Livraison de plats pour étudiants. Tous droits réservés.</p>
    </div>
</footer>
{% endblock %}
