{% extends 'base.html.twig' %}

{% block title %}LignePanier{% endblock %}

{% block body %}
    <h1>LignePanier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_panier.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_panier.quantite }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ligne_panier.createdAt ? ligne_panier.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne_panier_index') }}">back to list</a>

    <a href="{{ path('app_ligne_panier_edit', {'id': ligne_panier.id}) }}">edit</a>

    {{ include('ligne_panier/_delete_form.html.twig') }}
{% endblock %}
